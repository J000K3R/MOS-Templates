# 📦 J000K3R's MOS Templates

Templates for the **MOS Hub** – a collection of templates that are **directly discoverable inside the MOS Hub**.

These templates allow users to publish **Docker containers, plugins, and docker-compose setups** so they can be easily found, browsed, and reused by MOS users.

---

## 🚀 Overview

MOS Templates are designed to integrate directly with the **MOS Hub** and provide:

* ✔️ Discoverable **Docker templates** inside the MOS Hub
* ✔️ Reusable **plugin templates** for MOS extensions
* ✔️ Ready-to-use **docker-compose stacks** for common services
* ✔️ A standardized way to publish and maintain templates

> Goal: Make it easy to share, discover, and reuse MOS-compatible templates via the MOS Hub. 💡

---

## 📂 Repository Structure

Typical structure:

```
docker/            # Docker container templates for MOS Hub
compose/           # docker-compose stacks shown in MOS Hub
plugins/           # MOS plugin templates
icons/             # Icons and graphical assets for Hub display
maintainer.json    # Maintainer & metadata information
```

Each template is structured so it can be indexed and displayed correctly by the MOS Hub.

---

## ✨ Features

* 🔍 **Hub Discoverability**
  Templates are automatically discoverable and browsable inside the MOS Hub.

* 🐳 **Docker & Compose Ready**
  Publish Docker containers and full docker-compose stacks with minimal effort.

* 🧩 **Plugin Support**
  Create and distribute MOS plugins using a consistent template structure.
